# Kosong = same-origin (Vite proxy ke backend, cocok untuk testing dari HP).
# Isi hanya jika frontend di-host terpisah dari API, mis. http://localhost:3001
VITE_API_BASE=
# Google OAuth Client ID (console.cloud.google.com) — kosong = demo picker
VITE_GOOGLE_CLIENT_ID=
